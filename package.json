{
  "repository": "github:nd-toolkit/ndk-source",
  "license": "Unlicense",
  "scripts": {
    "eslint": "eslint . --max-warnings 0",
    "eslintcc": "eslintcc . --greater-than C --show-rules",
    "test": "node test",
    "link-for-test": "ndk-ci-cd link-for-test @ndk test",
    "test-and-coverage": "(nyc node test || node test) && (nyc check-coverage || nyc report -r=text && nyc check-coverage)",
    "coverage-html": "nyc -r=html node test",
    "build": "ndk-ci-cd build-multi-package multi-package.json"
  },
  "dependencies": {
    "@nd-toolkit/eslint-config": "latest",
    "@nd-toolkit/ci-cd": "latest",
    "eslint": "latest",
    "eslint-plugin-jsdoc": "latest",
    "eslintcc": "latest",
    "nyc": "latest"
  },
  "eslintIgnore": ["test/node_modules"],
  "nyc": {
    "all": true,
    "branches": 100,
    "lines": 100,
    "functions": 100,
    "statements": 100,
    "exclude": [
      "bin",
      "build",
      "test/index.js"
    ],
    "reporter": [
      "lcovonly"
    ],
    "temp-dir": "./build/nyc_output",
    "report-dir": "./build/coverage"
  }
}